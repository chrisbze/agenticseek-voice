services:
  - type: web
    name: agenticseek-api
    env: go
    buildCommand: go build -o agenticseek-voice
    startCommand: ./agenticseek-voice
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8080